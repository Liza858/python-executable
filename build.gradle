plugins {
    id 'application'
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

application {
    mainClassName = 'Main'
}

run {
    standardInput = System.in
    standardOutput = System.out
}

repositories {
    mavenCentral()
}